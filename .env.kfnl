# DSpace API Configuration - KFNL Production Servers
# Servers: 172.16.105.30, 10.105.3.10, kfnlrepo.kfnl.gov.sa
# Vue runs on port 80, DSpace backend on port 8080

# API URL - Using relative path so it works with all server IPs/domains
# The request will go to the same host that serves the Vue app
VITE_API_URL=/server/api

# Alternative: If DSpace is on a specific server, use one of these:
# VITE_API_URL=http://172.16.105.30:8080/server/api
# VITE_API_URL=http://10.105.3.10:8080/server/api
# VITE_API_URL=http://kfnlrepo.kfnl.gov.sa:8080/server/api

# Auto-login DISABLED in production for security
VITE_AUTO_LOGIN=false
VITE_DSPACE_USER=
VITE_DSPACE_PASSWORD=

# Application Settings
VITE_APP_TITLE=المستودع الرقمي - مكتبة الملك فهد الوطنية
VITE_APP_TITLE_EN=Digital Repository - King Fahd National Library

# Default Language
VITE_DEFAULT_LOCALE=ar

# Features - Debug disabled in production
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=false
